# Copy this to .env and adjust values as needed.
# These environment variables override config.json.

# Matching tolerances
TIMESTAMP_TOLERANCE_SECONDS=300
QUANTITY_TOLERANCE_PCT=0.01

# Database
# DB_DRIVER=memory          # default (no external dependency)
# DB_DRIVER=mongodb
# MONGODB_URI=mongodb://localhost:27017/reconciliation

# Server
PORT=3000
